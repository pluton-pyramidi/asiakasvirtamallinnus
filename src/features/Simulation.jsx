import * as React from "react";
import { Container, Box, Typography, TextField, Input } from "@mui/material";

export default function Simulation() {
  return <Container id="simulation-container"></Container>;
}
